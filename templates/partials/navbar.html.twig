
{% block nav %}
	<nav class="navbar navbar-expand-lg bg-body-tertiary">
		<div class="container-fluid">
			<a class="navbar-brand" href="{{ path('app_main')}}">Home
			</a>
			<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
				<span class="navbar-toggler-icon"></span>
			</button>
			<div class="collapse navbar-collapse" id="navbarNav">
				<ul class="navbar-nav mx-auto">
					<li class="nav-item">
						<a class="nav-link" href="{{ path('graine_liste')}}">Graines</a>
					</li>
					<li class="nav-item">
						<a class="nav-link" href="{{ path('atelier_liste')}}">Atéliers</a>
					</li>
					<li class="nav-item">
						<a class="nav-link" href="{{ path('actualite_liste')}}">Actualités
						</a>
					</li>
					{# {% if is_granted (["ROLE_CLIENT"]) %} #}
					{% if app.user %}
					<li class="nav-item">
						{# <a class="nav-link" href="{{ path('profile_detail' , {id:user.id})}}">Mon compte</a> #}
					</li>
					<li class="nav-item">
						<a class="nav-link" href="#">Panier</a>
					</li>
					<li class="nav-item ">
						<a class="nav-link " href="{{ path('app_logout') }}">Logout</a>
					</li>
				{% else %}
					<li class="nav-item">
						<a class="nav-link" href="{{ path('app_login')}}">Se connecter</a>
					</li>
				{% endif %}
				<ul class="navbar-nav ms-auto">
					<li class='nav-item'>
						<a class="nav-link disabled">Admin</a>
					</li>
				</ul>
			</ul>
		</div>
	</div>
</nav>{% endblock %}
