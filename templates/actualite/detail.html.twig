{% extends 'base.html.twig' %}

{% block title %}
	{{actualite.nom}}
{% endblock %}

{% block body %}
	<style>

		.img-fluid {
			width: 655px;
			height: 250px;
		}

		h2 {
			 background-color:  #04B45F;
			 text-decoration: none;
			 border-radius:10px;
		}
	</style>

	<div class="container-fluid">
		<div class="row my-3 mx-3 ">

			<h2 class="text-center">
				{{actualite.nom}}
				<h2></div>

				
					<div class="d-flex justify-content-center my-2 ">
						<img class="img-fluid" src="{{ vich_uploader_asset(actualite, 'imageFile') }}" alt="{{ actualite.nom }}"/></li>
				</div>
			<p>	
				{{actualite.description}}
				</p> 
			</div>

		</div>
	{% endblock %}
