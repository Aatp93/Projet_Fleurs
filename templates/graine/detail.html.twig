{% extends 'base.html.twig' %}
{% block stylesheet %}
	<link rel="stylesheet" href="{{ asset('assets/css/detail.graine.css') }}">
{% endblock %}

{% block title %}
	{{graine.nom}}
{% endblock %}

{% block body %}
	<style>
		a {

			text-decoration: none;
			color: black;
		}

		.btn-custom {
			background-color: #04B45F;
		}

		h2 {
			background-color: #04B45F;
			color: black;
			border-radius:10px;
		}

			h3 {
			background-color: #04B45F;
			color: black;
			border-radius:10px;
		}
	</style>

	<div class="container-fluid text-center">

		<h2 class="text-center">
			{{graine.nom}}
		</h2>

		<div class="row ">
			<div class="col-6 ">
				<img class="img-fluid" src="{{ vich_uploader_asset(graine, 'imageFile') }}" alt="{{ graine.nom }}"/>

			</div>

			<div class="col-6  align-self-center">
				<h3>
					Informations
				</h3>
				<br>
			</br>

			<h5>
				<i class="fas fa-weight-hanging"></i>
				Poids sachet :
				{{graine.poid ~' '~ 'g'}}

			</h5>

			
			<h5>
				<i class="fas fa-clover"></i>
				Periode de plantation :
				{{graine.saison}}


			</h5>

			<h5>
				<i class="fas fa-euro-sign"></i>
				Prix :
				{{graine.prix ~'€'}}

			</h5>

			<h5 class="">
				<i class="fas fa-file-lines"></i>
				
				{{graine.description}}

			</h5>

			<br> 
			{% if is_granted("ROLE_CLIENT") %}
			<a class="btn-custom btn " href="{{ path('panier_add', {id:graine.id} )}}" role="button">
				<h4>
					Ajouter au panier
				</h4>
			</a>
			{% endif %}

		</button>
	</div>
</div></div></div>{% endblock %}
